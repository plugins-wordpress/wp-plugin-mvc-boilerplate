'use strict';
// Render the FAQ page view
exports.index = (req, res, next) => {
    // This function is used to render the FAQ page view, typically containing frequently asked questions and their answers.
    res.render('pages-faq');
  };
  