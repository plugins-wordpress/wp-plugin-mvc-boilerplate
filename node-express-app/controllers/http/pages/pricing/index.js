'use strict';
// Render the pricing page view
exports.index = (req, res, next) => {
    // This function is used to render the pricing page view, typically containing information about pricing plans or options.
    res.render('pages-pricing');
  };
  