'use strict';
// Exported function to render the access roles view

exports.index = (req, res, next) => {
    // This function is used to render the access roles view, typically associated with managing user roles and permissions.
    res.render('app-access-roles');
  };
  